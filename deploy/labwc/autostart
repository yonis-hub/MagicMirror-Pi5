#!/bin/sh
# User-level labwc autostart for MagicMirror Pi 5.
# Pi OS's system default (/etc/xdg/labwc/autostart) is loaded separately by
# labwc and handles panels, file manager desktop, kanshi, and XDG autostart.
#
# Both the MagicMirror server (mm-server.service) and the kiosk Chromium
# (mm-kiosk.service) are user-systemd units now. This file is intentionally
# minimal — left around in case we ever need a quick session-time hook.

exit 0
